namespace KSpirits.UI
{
    /// <summary>
    /// UI 영역별 폰트 역할. UIFontSettings 에서 Font 를 연결한다.
    /// </summary>
    public enum UIFontRole
    {
        Default = 0,
        Dialogue = 1,
        UserInfo = 2,
        HudNumeric = 3,
    }
}
